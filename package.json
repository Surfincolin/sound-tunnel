{
  "name": "sound-tunnel",
  "version": "1.0.0",
  "description": "A server that hosts a web page with socket.io to allow for sound playback through touchs on an iPad.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Colin Wageman",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.3",
    "socket.io": "^1.3.7",
    "speaker": "^0.2.6",
    "wav": "^1.0.0"
  }
}
